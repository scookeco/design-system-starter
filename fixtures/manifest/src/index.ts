export { Widget, type WidgetProps, type WidgetTone } from './components/Widget/Widget';
export { Leaky } from './components/Leaky/Leaky';

/** Formats a count. */
export const useCount = (count: number): string => String(count);
